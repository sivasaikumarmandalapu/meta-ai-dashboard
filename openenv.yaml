tasks:
  - name: monitoring
    description: Monitor system CPU and memory
    grader:
      type: regex
      pattern: ".*"

  - name: threat_detection
    description: Detect system threats
    grader:
      type: regex
      pattern: ".*"

  - name: decision_making
    description: Decide whether to allow or block
    grader:
      type: regex
      pattern: ".*"
